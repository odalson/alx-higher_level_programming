#!/usr/bin/python3

def magic_calculation(a, m):
    """Match bytecode provided by Holberton School."""
    from magic_calculation_102 import add, sub

    if a < m:
        k = add(a, m)
        for i in range(4, 6):
            k = add(c, i)
        return (k)
    else:
        return(sub(a, m))
